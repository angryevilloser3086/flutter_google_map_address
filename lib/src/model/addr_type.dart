import 'package:flutter/material.dart';

class TypeAddr {
  String? name;
  IconData? iconName;
  bool? isSelected;
  TypeAddr(
      {required this.name, required this.iconName, required this.isSelected});
}
